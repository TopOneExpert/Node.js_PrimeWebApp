/*
 * CustomAccordion Messages
 *
 * This contains all the text for the CustomAccordion component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CustomAccordion';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomAccordion component!',
  },
});
