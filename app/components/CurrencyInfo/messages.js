/*
 * CurrencyInfo Messages
 *
 * This contains all the text for the CurrencyInfo component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CurrencyInfo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CurrencyInfo component!',
  },
});
