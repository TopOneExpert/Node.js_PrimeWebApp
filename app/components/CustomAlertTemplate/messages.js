/*
 * CustomAlertTemplate Messages
 *
 * This contains all the text for the CustomAlertTemplate component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CustomAlertTemplate';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomAlertTemplate component!',
  },
});
