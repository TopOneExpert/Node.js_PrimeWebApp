/*
 * EmailVerification Messages
 *
 * This contains all the text for the EmailVerification component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EmailVerification';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EmailVerification component!',
  },
});
