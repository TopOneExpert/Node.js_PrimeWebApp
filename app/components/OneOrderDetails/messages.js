/*
 * OneOrderDetails Messages
 *
 * This contains all the text for the OneOrderDetails component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.OneOrderDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OneOrderDetails component!',
  },
});
