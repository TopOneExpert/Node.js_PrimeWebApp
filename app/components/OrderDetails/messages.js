/*
 * OrderDetails Messages
 *
 * This contains all the text for the OrderDetails component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.OrderDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OrderDetails component!',
  },
});
