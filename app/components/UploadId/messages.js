/*
 * UploadId Messages
 *
 * This contains all the text for the UploadId component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.UploadId';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UploadId component!',
  },
});
