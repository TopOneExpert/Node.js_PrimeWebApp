/*
 * CreateNewOrder Messages
 *
 * This contains all the text for the CreateNewOrder component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CreateNewOrder';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CreateNewOrder component!',
  },
});
