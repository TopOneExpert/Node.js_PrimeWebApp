/*
 * EditableInput Messages
 *
 * This contains all the text for the EditableInput component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EditableInput';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EditableInput component!',
  },
});
