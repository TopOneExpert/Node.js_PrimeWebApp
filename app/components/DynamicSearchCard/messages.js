/*
 * DynamicSearchCard Messages
 *
 * This contains all the text for the DynamicSearchCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DynamicSearchCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DynamicSearchCard component!',
  },
});
