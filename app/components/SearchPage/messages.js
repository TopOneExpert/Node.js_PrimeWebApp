/*
 * SearchPage Messages
 *
 * This contains all the text for the SearchPage component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SearchPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SearchPage component!',
  },
});
