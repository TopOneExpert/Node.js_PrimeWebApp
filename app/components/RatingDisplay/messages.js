/*
 * RatingDisplay Messages
 *
 * This contains all the text for the RatingDisplay component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RatingDisplay';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RatingDisplay component!',
  },
});
