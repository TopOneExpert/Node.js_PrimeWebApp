/*
 * CustomToggle Messages
 *
 * This contains all the text for the CustomToggle component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CustomToggle';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomToggle component!',
  },
});
